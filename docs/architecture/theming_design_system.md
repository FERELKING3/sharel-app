Date: 2026-02-06
Version: 1.0.0+1
Author: generated by agent

# Theming & Design System

Le design system est implémenté dans `lib/core/theme/design_system.dart` et expose `AppTheme` et les `ThemeData`.

Principes:

- Material 3 : composantes modernes, échelle typographique centralisée.
- Variables globales : `AppTheme.spacing16`, `spacing8`, etc.
- Les composants partagés sont placés dans `lib/widgets/` (ex: `large_action_button`, `app_shell`).

Conseil : garder styles et tokens (couleurs, tailles) dans `design_system.dart` pour éviter duplication.
