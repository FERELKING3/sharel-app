Date: 2026-02-06
Version: 1.0.0+1
Author: generated by agent

# Discovery

Actuellement la découverte est basique (scan réseau, UI `DiscoveryScreen`). Pour fiabiliser :

Approches recommandées :

- mDNS / Bonjour : annonce du service `_sharel._tcp.local` avec payload minimal (`ip,port,sessionId,protocolVersion`).
- Fallback QR : le QR code encode `http://<ip>:<port>?session=<sessionId>&t=<token>` pour copy/paste ou scan.

Gestion des collisions : si plusieurs rooms répondent, afficher liste avec `transferId`, `name`, `peer info` et permettre sélection manuelle.
